#include<stdio.h>
#include<stdlib.h>

struct arvore{
	int chave;
	struct arvore *esq;
	struct arvore *dir;
};
typedef struct arvore no; //definindo essa struct como nó, para ficar mais rápido de escrever


void inserir (no *raiz, int k){//função inserção
	
}
